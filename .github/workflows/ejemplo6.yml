name: Creando-usando Artifacts
on:
  push:
    branches:
      - develop
      - main
  pull_request:
    branches:
      - develop
      - main
jobs:
  creando-artifact:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Configurando JDK
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: Compilando el *.jar
        run: ./mvnw clean package -DskipTests
        env:
          SPRING_PROFILES_ACTIVE: dev
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      - name: Subir JAR a workflow artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: springboot-app
          path: target/*.jar
      - name: Bajar JAR de workflow artifacts
        uses: actions/download-artifact@v8.0.1
        with:
          name: springboot-app
          path: ./jars
      - name: Mostrar JAR(s) descargado(s)
        run: ls -R ./jars
  creando-assets:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Configurando JDK
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '25'
          distribution: 'temurin'
      - name: Compilando el *.jar
        run: ./mvnw clean package -DskipTests
        env:
          SPRING_PROFILES_ACTIVE: dev
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      - name: Subir a GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v1.11.2
          files: target/*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}